pipeline{

	agent any

	environment {
		DOCKERHUB_CREDENTIALS=credentials('dockerhub-yag')
	}

	stages {

		stage('Build') {

			steps {
				sh 'docker build -t website8 .'
			}
		}

		stage('Login') {

			steps {
				sh 'echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'
			}
		}

		stage('Push') {

			steps {
			        sh'docker tag website8 yagvendrathakur/personal-website'
				sh 'docker push yagvendrathakur/personal-website'
			}
		}
	}

	post {
		always {
			sh 'docker logout'
		}
	}

}
